---
title: "Kubernetes 입문: 컨테이너 오케스트레이션의 세계"
description: "Kubernetes의 핵심 개념인 Pod, Service, Deployment를 이해하고, 왜 K8s가 필요한지 알아봅니다."
date: "2026-02-04"
category: "DevOps"
tags: ["kubernetes", "k8s", "devops", "container"]
thumbnail: "/images/posts/kubernetes-intro.svg"
series:
  name: "Docker 완전 정복"
  order: 3
---

## Kubernetes란?

Kubernetes(K8s)는 컨테이너화된 워크로드와 서비스를 관리하기 위한 **오케스트레이션 플랫폼**입니다.

> Docker가 컨테이너를 만들고 실행한다면, Kubernetes는 수백 개의 컨테이너를 조율합니다.

## 핵심 개념

### Pod

Pod는 K8s에서 배포 가능한 **가장 작은 단위**입니다.

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: my-app
spec:
  containers:
    - name: app
      image: my-app:latest
      ports:
        - containerPort: 3000
```

### Deployment

Deployment는 Pod의 **선언적 업데이트**를 관리합니다.

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: my-app
  template:
    metadata:
      labels:
        app: my-app
    spec:
      containers:
        - name: app
          image: my-app:latest
```

### Service

Service는 Pod 집합에 대한 **네트워크 접근을 추상화**합니다.

```yaml
apiVersion: v1
kind: Service
metadata:
  name: my-app-svc
spec:
  selector:
    app: my-app
  ports:
    - port: 80
      targetPort: 3000
  type: ClusterIP
```

## kubectl 기본 명령어

| 명령어 | 설명 |
|--------|------|
| `kubectl get pods` | Pod 목록 |
| `kubectl apply -f` | 매니페스트 적용 |
| `kubectl logs` | 로그 확인 |
| `kubectl describe` | 리소스 상세 정보 |
| `kubectl delete` | 리소스 삭제 |

## Docker Compose vs Kubernetes

- **Docker Compose**: 단일 호스트, 개발 환경에 적합
- **Kubernetes**: 멀티 노드, 프로덕션 환경에 적합, 자동 복구/스케일링
